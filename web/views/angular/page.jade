
div(style="float:left;height:80px")
  h1 Page

#newItem(style="height:80px;width:80px;float:left;margin-top:30px;margin-left:20px")
  .editing_off(ng-hide="addingItem" style="float:left")
    button(ng-click="addingItem=true", ) Add Item
  .editing_on(ng-show="addingItem" style="width:340px;float:left")
    input(style="width:280px;height:22px;float:left", ng-model="newItemUrl", ng-show="addingItem", placeholder="Enter new item URL")
    button(ng-click="addItem(newItemUrl); addingItem=false", style="height:24px;float:left") OK

.clear

p(ng-show="page.items.length == 0") There are no items on this page, add some!

//div(ui-sortable ng-model="page.items")
//  trip-item(ng-repeat="item in page.items" item="item" delete-item="deleteItem(itemId)")

div(ui-sortable ng-model="page.items")
    div(ng-repeat="item in page.items" item="item", style="height:200px; width:200px; border:1px solid")
      .page_item(ng-mouseenter='editing=true', ng-mouseleave='editing=false')
        article.page_item_content
          .page_item_header
            div(ng-show='editing', ng-click='deleteItem({itemId:item._id})')
            span Delete
          .page_item_body
            .page_item_image_wrap(ng-show='ng-show', item.images.length='item.images.length')
              | 0"
              img.page_item_image(ng-src='{{item.images[0].url}}')
          .page_item_footer
            .page_item_title
              p
                a(href='{{item.url}}', title='{{item.title}}')  {{item.title}}
